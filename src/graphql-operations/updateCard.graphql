mutation updateCard(
  $boardId: ID
  $columnId: ID
  $description: String
  $cardId: ID!
  $priority: Int
  $title: String
) {
  updateCard(
    boardId: $boardId
    columnId: $columnId
    id: $cardId
    priority: $priority
    title: $title
    description: $description
  ) {
    id
  }
}
