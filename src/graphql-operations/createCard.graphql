mutation createCard(
  $boardId: ID!
  $description: String
  $priority: Int!
  $title: String!
) {
  createCard(
    boardId: $boardId
    priority: $priority
    title: $title
    description: $description
  ) {
    id
  }
}
