mutation createCard(
  $card: CardInput!
  $cardId: ID!
  $cardIds: [ID!]!
  $workflowStateId: ID!
) {
  updateCard(id: $cardId, Card: $card) {
    id
  }
  updateWorkflowState(id: $workflowStateId, cardIds: $cardIds) {
    id
  }
}
