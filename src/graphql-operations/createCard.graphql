mutation createCard(
  $card: CardInput!
  $cardId: ID!
  $cardIds: [ID!]!
  $columnId: ID!
) {
  updateCard(id: $cardId, Card: $card) {
    id
  }
  updateColumn(id: $columnId, cardIds: $cardIds) {
    id
  }
}
