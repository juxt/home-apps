query kanbanData {
  allBoards {
    name
    description
    id
    columns {
      id
      name
      cards {
        id
        priority
        title
      }
    }
  }
  allColumns {
    id
    name
  }
}
