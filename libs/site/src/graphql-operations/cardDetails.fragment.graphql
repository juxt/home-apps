fragment CardDetails on Card {
  ...CardFields
  ... on HiringCard {
    description
    agent
    _siteSubject
    location
    hasRemoteFee
    taskHtml
    files {
      base64
      name
      type
    }
    workflowState {
      id
      name
      tasks
      roles
    }
    stateStr
  }
}
