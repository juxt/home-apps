query allReviews {
  allTVFilmReviews {
    ...reviewFields
  }
}

query reviewById($tmdb_id: ID!) {
  tvFilmReviewsById(id: $tmdb_id) {
    ...reviewFields
  }
}

mutation upsertReview($TVFilmReview: TVFilmReviewInput!) {
  createTVFilmReview(TVFilmReview: $TVFilmReview) {
    id
  }
}

mutation deleteReview($id: ID!) {
  deleteTVFilmReview(id: $id) {
    id
  }
}

fragment reviewFields on TVFilmReview {
  _siteCreatedAt
  _siteSubject
  _siteValidTime
  id
  tmdb_id
  reviewHTML
  score
}
