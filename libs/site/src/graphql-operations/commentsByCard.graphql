query commentsForCard($id: ID!) {
  commentsForEntity(id: $id, limit: 100, order: "asc") {
    id
    _siteCreatedAt
    _siteSubject
    _siteValidTime
    text
    parentId
    children {
      text
    }
  }
}
